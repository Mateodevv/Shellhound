# Runtime dependencies for the server package. Keep these aligned with
# project.dependencies in pyproject.toml; frontend packages live in web/.
fastapi>=0.115
uvicorn[standard]>=0.30
# Country attribution for IPs -- reads a LOCAL MMDB from the workspace
# (DB-IP Lite via a button in the interface, or GeoLite2 by hand). Without
# the package everything runs, only without flags.
maxminddb>=2.5
# REQUIRED, not optional. The web shell content rules ARE YARA rules
# (server/rules_bundled/); without this package the scan would silently find
# thirteen fewer things, which is worse than failing to start. It was
# optional while YARA only ran the analyst's own rules.
yara-python>=4.3
# SIGMA rules are YAML. Required since the log side reads them
# (server/sigma.py); it used to arrive only as a transitive extra of
# uvicorn[standard], which is not something to depend on.
PyYAML>=6.0
# Named source time zones on Windows; bundled for offline use after setup.
tzdata>=2025.2
